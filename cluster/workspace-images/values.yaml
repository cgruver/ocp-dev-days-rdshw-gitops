# Default values for cekit-builder chart
nameOverride: ""

# Git repository configuration
git:
  uri: "https://github.com/cgruver/opinionated-dev-spaces.git"
  ref: "main"
  contextDir: "managed-images/cekit-builder-image"

# Image registry configuration
registry:
  hostname: "image-registry.openshift-image-registry.svc:5000"
  namespace: "cekit-images"
  # The builder image will be built and pushed here
  builderImage: "cekit-builder"
  builderTag: "latest"

# Resource limits for builds and tasks
resources:
  limits:
    cpu: "4"
    memory: "4Gi"
  requests:
    cpu: "500m"
    memory: "512Mi"

# Additional labels to add to all resources
labels: {}

# Annotations for ArgoCD sync-wave and hooks
annotations: {}